/// Maximum number of pages that can be scanned at once.
/// Increase this value to allow more pages per scan session.
const int maxScanPages = 5;

/// Image quality for the image picker (0-100).
const int scanImageQuality = 85;
